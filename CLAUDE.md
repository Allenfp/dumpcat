# Claude Code Configuration

## Permissions

This is a secure local development environment. Claude is authorized to:

- Run any shell commands needed for development (install packages, run builds, start servers, etc.)
- Read, create, edit, and delete any files in the project
- Run tests and linters
- Manage git operations (commit, branch, merge)
- Install and manage dependencies
- Start and stop local services and processes
- Access local network services (databases, APIs, dev servers)

No need to ask for confirmation on local development actions.
